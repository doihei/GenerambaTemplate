//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import Reusable
import RxSwift
import RxCocoa
import UIKit

final class {{ module_info.name }}ViewController: UIViewController, StoryboardBased {

    private lazy var viewModel = undefined()
    
    override func viewDidLoad() {
        super.viewDidLoad()
        bindInput()
        bindOutput()
    }
}

// MARK: - bind input
extensiton {{ module_info.name }}ViewController {

    private func bindInput() {
        /*
         *  EXAMPLE:
         *
         *  let input = viewModel.input
         *
         *  button.rx.tap
         *      .bind(to: input.accept(for: \.buttonTap))
         *      .disposed(by: disposeBag)
         */
    }
}

// MARK: - bind output
extensiton {{ module_info.name }}ViewController {
    
    private func bindOutput() {
        /*
         *  EXAMPLE:
         *
         *  let output = viewModel.output
         *
         *  output.observable(for: \.isEnabled)
         *      .bind(to: button.rx.isEnabled)
         *      .disposed(by: disposeBag)
         *
         *  print("rawValue of isEnabled = \(output.value(for: \.isEnabled))")
         */
    }
}